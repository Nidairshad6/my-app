version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 875681151031.dkr.ecr.us-east-1.amazonaws.com



  build:
    commands:
      - echo Building Docker image...
      - docker login -u nidairshad -p dckr_pat_3VN9xYfXWU3j-rLpP7MNHa0vY18

      - docker build -t my-repo:latest .
      - docker tag my-repo:latest 875681151031.dkr.ecr.us-east-1.amazonaws.com/my-repo:latest

  post_build:
    commands:
      - echo Build completed on `date`
      - echo Pushing the Docker image...
      - docker push public.ecr.aws/y1w7a2c8/my-repo


artifacts:
  files:
    - '**/*'